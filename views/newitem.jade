extends ./layout.jade
block form
    form(action="/poll/" + poll.user + "/" + poll.id + "/add", method="post")
        div(class="form-group")
            label Item title
            input(type="text", name="title", class="form-control")
        div(class="form-group")
            button(type="submit", class="btn btn-default") Add
